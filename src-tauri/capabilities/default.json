{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main ProseMap window can only invoke audited application commands.",
  "windows": ["main"],
  "permissions": ["core:event:default", "core:window:allow-destroy"]
}
